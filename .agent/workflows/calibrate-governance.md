---
description: calibrate-governance: Supreme project calibration protocol for GDD and boundary integrity
---

# Governance Calibration Protocol 🏛️

This workflow is the **Antigravity Native Command** for calibrating the project's documentation, architectural alignment, and AI-agent hygiene. It enforces the **Strict-Mandatory** protocol.

## 0. Pre-computation Cognitive Boundary
**CRITICAL**: Before executing this workflow, you MUST read the central architecture standard Knowledge Items (KIs) located in `<appDataDir>/knowledge/` and cross-reference with:
`view_file` -> `.agent/skills/slotify-architecture-standards/SKILL.md`

## 1. Calibration Anchors

The agent MUST ensure all files align with these three anchors:
- **GDD (Guideline-Driven Development)**: The default development lifecycle governed by Antigravity-native boundaries.
- **Protection Boundary (保護邊界)**: The absolute isolation of instruction context.
- **English-First Policy**: All core guidance MUST be in English for global maintainability.

## 2. Terminology Accuracy

Audit all `.md` files in the root and `.agent/workflows/`. Ensure zero "soft" language.
- **REPLACE**: "Should" → **MUST**
- **REPLACE**: "Can" → **MAY** (if optional) or **SHALL** (if architectural)
- **REPLACE**: "Development plan" → **GDD Alignment Check**
- **REPLACE**: "Demo content" → **Isolated Demo Content**

## 3. Section Order Optimization

Force-verify the **Universal → Proprietary** hierarchy in `README.md` and KIs:
1.  **AI & Core Architecture**: Universal principles (now dissolved into KIs and `CONTRIBUTING.md`).
2.  **Implementation & Features**: Proprietary/Specific details for human users (in `README.md`).

## 4. Boundary Lockdown

- **ACTION**: Verify `.antigravityignore` contains `archetypes/` and `exampleSite/`.
- **STICT PROHIBITION**: AI agents SHALL NOT reference Markdown content from these directories to justify architectural decisions. Any such reference is a CALIBRATION FAILURE.

## 5. Execution Steps

1.  **Terminology Scan**: Identify and upgrade all soft directives to mandatory ones in KIs and skills.
2.  **Language Audit**: Translate any non-compliant Chinese sections in core instructions to English.
3.  **Structural Audit**: Verify the structure of `README.md` and `CONTRIBUTING.md`.
4.  **Verification**: Confirm `.antigravityignore` is effective.
