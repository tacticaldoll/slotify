{
  "name": "slotify-theme-tests",
  "version": "0.2.1",
  "private": true,
  "description": "Browser smoke tests for the Slotify Hugo theme (Playwright).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
